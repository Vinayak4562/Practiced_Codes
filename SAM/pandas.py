#Its a python lib. To handel or manupelate the tabuler date we use panda library.
# we can hadei 2 typ of data 1) Data series(Col of data frame) 2) data frame (combination of rows and col).
# excell n data frame are same in terms of showing n sorting the data.
#
# Creating of Data series

import pandas as pd

a = ["India", "Pakistan", 22, 45, 2.32]
# pd.Series(a)
# pd.Series(a)
print(pd.Series(a))